version: 1
updated: 2026-03-26

# Master index of all marketplace items.
# Each entry is a summary — full metadata lives in the item's manifest.yaml.
# Fields: id, type, name, version, description, author, tags, path
# Type-specific extra fields (event, command, merge_key) are included where relevant.

items:
  - id: blinko
    type: skill
    name: Blinko
    version: 1.0.0
    description: Make direct API calls to a self-hosted Blinko instance via curl. Supports notes, tags, files, user info, config, and analytics.
    author: schumannjon
    tags: [blinko, notes, api, self-hosted]
    path: skills/blinko
